// index.js

import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // Update the import path to point to the correct location

ReactDOM.render(<App />, document.getElementById('root'));